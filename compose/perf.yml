services:
  perf:
    build:
      context: ..
      dockerfile: containers/perf/Dockerfile
    image: grimace-py-perf:local
    working_dir: /build-src
    user: "${LOCAL_UID:?Set LOCAL_UID through Makefile}:${LOCAL_GID:?Set LOCAL_GID through Makefile}"
    network_mode: "none"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 1024
    mem_limit: 2g
    tmpfs:
      - /tmp:size=1024m,mode=1777
    environment:
      RUN_PERF_TESTS: "1"
      GRIMACE_PERF_GIT_COMMIT: "${GRIMACE_PERF_GIT_COMMIT:-}"
      GRIMACE_PERF_GIT_CHANGE: "${GRIMACE_PERF_GIT_CHANGE:-}"
      GRIMACE_PERF_GIT_DIRTY: "${GRIMACE_PERF_GIT_DIRTY:-}"
      MPLCONFIGDIR: /tmp/matplotlib
    volumes:
      - type: bind
        source: ../docs/timings.tsv
        target: /build-src/docs/timings.tsv
        bind:
          create_host_path: false
      - type: bind
        source: ../docs/timings.md
        target: /build-src/docs/timings.md
        bind:
          create_host_path: false
      - type: bind
        source: ../docs/timing-plots
        target: /build-src/docs/timing-plots
        bind:
          create_host_path: false
      - type: bind
        source: ../notes/004_perf_history.jsonl
        target: /build-src/notes/004_perf_history.jsonl
        bind:
          create_host_path: false
