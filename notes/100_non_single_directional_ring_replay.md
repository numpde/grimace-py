# Non-single directional ring replay

The scoped double-ring carrier is one coupled semantic relation: artifact policy
identity supplies the `""`/`"="` endpoint domain, exactly one endpoint owns the
marker, and the directional carrier is restricted to `ABSENT`.

Opening and pairing reuse the v7 transition terms and the existing residual
mutation. Offline replay derives endpoint choices from prepared identity rather
than invoking live policy helpers. Coupled closure lifecycle evidence receives
credit only after residual and linked stereo lifecycle replay succeeded.

This slice remains limited to one specified model on a non-bridge DOUBLE bond
with the ordinary non-directional endpoint domain. Shared carriers, triples,
custom domains, direct slash/backslash marks, and tetra ring ordering remain out
of scope.
