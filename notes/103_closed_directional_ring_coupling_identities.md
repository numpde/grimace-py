# Closed directional ring coupling identities

The v8 coupling proof now closes its remaining substitution surfaces. Structural
validation rejects extra top-level keys and invalid literal values. Offline
replay derives opening and closing roles from the graph event, includes the
closure label in manifest selection, requires the raw stereo lifecycle and the
phase-specific residual operation, and matches opening or closed ring records by
their full semantic identity.

Global replay sets still prove execution; branch-local indexes prove ownership.
No chemistry or runtime support surface changes in this slice.
